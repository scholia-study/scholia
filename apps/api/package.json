{
    "name": "@apps/api",
    "private": true,
    "scripts": {
        "dev": "cargo run -p api --bin api",
        "openapi": "cargo run -p api --bin openapi > ../../openapi.json && pnpm exec biome check --write ../../openapi.json",
        "test": "cargo test -p api"
    }
}
