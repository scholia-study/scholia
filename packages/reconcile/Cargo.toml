[package]
name = "reconcile"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
sqlx = { version = "0.8", features = ["runtime-tokio", "postgres", "uuid"] }
uuid = { version = "1", features = ["v4"] }
