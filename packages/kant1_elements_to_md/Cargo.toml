[package]
name = "kant1_elements_to_md"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../../lib/common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
glob = "0.3"
regex = "1"
