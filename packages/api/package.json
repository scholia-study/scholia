{
    "name": "api",
    "private": true,
    "scripts": {
        "dev": "cargo run -p api --bin api",
        "openapi": "cargo run -p api --bin openapi > ../../openapi.json && pnpx @biomejs/biome check --write ../../openapi.json",
        "test": "cargo test -p api"
    }
}
