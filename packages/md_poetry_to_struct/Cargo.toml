[package]
name = "md_poetry_to_struct"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
text_struct = { path = "../text_struct" }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
